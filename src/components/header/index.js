import './style.css';
import BtnUpdate from '../updateBtn';

const Header = () => {
  return (
    <header className="App-header">
      <div className="text-header">API REST Usinas</div>
      <BtnUpdate />
    </header>
  );
};

export default Header;
